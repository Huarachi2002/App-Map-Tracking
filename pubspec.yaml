name: app_map_tracking
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  connectivity_plus: ^6.1.4
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  geolocator: ^10.1.0
  go_router: ^15.1.1
  http: ^1.4.0
  maplibre_gl: ^0.20.0
  path_provider: ^2.1.5
  shared_preferences: ^2.5.3
  socket_io_client: ^3.1.2

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/maplibre/
    - assets/maplibre/sprites/
    - assets/maplibre/glyphs/
